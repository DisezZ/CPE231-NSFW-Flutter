import 'package:flutter/material.dart';

class EmployeeBranch extends StatelessWidget {
  const EmployeeBranch({Key? key}) : super(key: key);

  static Route route() {
    return MaterialPageRoute<void>(builder: (_) => EmployeeBranch());
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
